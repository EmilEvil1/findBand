import React from 'react';
import {Container, Grid} from "@material-ui/core";

const Home = () => {
    return (
        <Grid>
            <Container>
                HOME PAGE
            </Container>
        </Grid>
    );
};

export default Home;
