import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    wrapper: {
        display: "flex"
    },
    searchBtn: {
        borderRadius: 0
    },
    input: {
        background: "white",
        color: "black"
    }
});