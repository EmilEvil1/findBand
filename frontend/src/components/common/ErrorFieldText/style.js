import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    errorText: {
        position: "absolute",
        bottom: -25,
        width: '100%',
        color: '#f44336'
    }
});