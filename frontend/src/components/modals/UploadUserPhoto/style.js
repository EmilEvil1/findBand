import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    wrapper: {
        padding: 50,
    },
    title: {
        marginBottom: 40
    },
    cropContainer: {
        position: 'relative',
        width: '100%',
        height: 200,
        background: '#333'
    }
});