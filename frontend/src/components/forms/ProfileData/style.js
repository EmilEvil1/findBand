import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    wrapper: {
        width: '50%'
    },
    marginBlock: {
        marginBottom: 35,
    },
    marginContainer: {
        marginTop: 35
    }
});